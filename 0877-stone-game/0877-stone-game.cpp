class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        //alice always wins the game because in any of the turn he will always take the
        //number with the highest value. So he will always win the game.
        return true;
    }
};